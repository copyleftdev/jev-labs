\* Escalation enabled: UNCERTAIN is a first-class outcome.
\* EXPECTED: all properties HOLD, including liveness.
SPECIFICATION Spec

CONSTANTS
    Agents = {a1,a2,a3,a4,a5}
    Verdicts = {yes, no}
    Quorum = 3
    MaxAsks = 2
    MaxCrashes = 2
    EnableEscalation = TRUE

INVARIANTS
    TypeOK
    DecisionIsReproducible
    EscalationIsJustified

PROPERTIES
    EventuallyDecided
