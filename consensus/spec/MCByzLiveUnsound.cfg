\* The SAME liveness question at an UNSOUND quorum (Q=2 <= 2f=2).
\* V3 already proved this unsafe. Here we ask whether it is also bad for
\* liveness quality -- i.e. whether the sweep bound protects both.
\* EXPECTED: NoContradictionOfHonestQuorum VIOLATED (a forger flips it).
SPECIFICATION Spec
CONSTANTS
    Agents = {a1, a2, a3}
    Byzantine = {a3}
    Verdicts = {yes, no}
    Quorum = 2
    MaxAsks = 2
INVARIANTS
    TypeOK
    NoForcedEscalation
    NoContradictionOfHonestQuorum
PROPERTIES
    EventuallyDecided
