\* Same, but quorum 3 of 4: 2*Quorum = 6 > 4, so every pair of quorums
\* shares an agent and the two coordinators cannot diverge.
\* EXPECTED: all invariants HOLD.
SPECIFICATION Spec
CONSTANTS
    Agents = {a1, a2, a3, a4}
    Byzantine = {}
    Coordinators = {c1, c2}
    Verdicts = {yes, no}
    Quorum = 3
    MaxCrashes = 0
INVARIANTS
    TypeOK
    DecisionIsVerifiable
    Agreement
