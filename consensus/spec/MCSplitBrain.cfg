\* Two coordinators, quorum 2 of 4 agents, no liars.
\* 2*Quorum = 4 is NOT > 4, so disjoint quorums exist.
\* EXPECTED: Agreement VIOLATED (split brain).
SPECIFICATION Spec
CONSTANTS
    Agents = {a1, a2, a3, a4}
    Byzantine = {}
    Coordinators = {c1, c2}
    Verdicts = {yes, no}
    Quorum = 2
    MaxCrashes = 0
INVARIANTS
    TypeOK
    DecisionIsVerifiable
    Agreement
